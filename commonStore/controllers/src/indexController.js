angular.module('controllers.indexController',[]);
angular.module('controllers.indexController').controller('indexController',['$scope','$localStorage','$location',function($scope,$localStorage,$location){

	// toolbar.init();
	
	// // $scope.dashboardLink = links.dashboard;

	// $scope.logout = function(){

	// 	delete $localStorage.accessToken;
	// 	$location.path(paths.login);
	// }
}]);
